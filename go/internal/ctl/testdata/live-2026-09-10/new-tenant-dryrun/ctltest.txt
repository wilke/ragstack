== new-tenant plan: ctltest ==
RAG_DATA:    /tmp/ctl-capture-873331
RAG_IMAGES:  /tmp/ctl-capture-873331/images
tenant dir:  /tmp/ctl-capture-873331/tenants/ctltest
acl/registry store: sqlite
port block:  base 41000, stride 20 (new allocation, index 0)

-- ports --
api:            41000
qdrant http:    41001
qdrant grpc:    41002
es http:        41003
es transport:   41004
postgres:       41005 (reserved, unused — sqlite default / shared server via --postgres)

-- manifest row (/tmp/ctl-capture-873331/tenants/manifest.tsv) --
ctltest	0	41000

-- directories (mkdir -p; every writable path enumerated — house rule: no tmpfs overlays) --
/tmp/ctl-capture-873331/tenants/ctltest/qdrant/storage
/tmp/ctl-capture-873331/tenants/ctltest/qdrant/snapshots
/tmp/ctl-capture-873331/tenants/ctltest/elasticsearch/data
/tmp/ctl-capture-873331/tenants/ctltest/elasticsearch/logs
/tmp/ctl-capture-873331/tenants/ctltest/elasticsearch/config
/tmp/ctl-capture-873331/tenants/ctltest/state
/tmp/ctl-capture-873331/tenants/ctltest/manifests
/tmp/ctl-capture-873331/tenants/ctltest/ingest
/tmp/ctl-capture-873331/tenants/ctltest/config
/tmp/ctl-capture-873331/tenants/ctltest/bin

-- required images (shared SIFs, reused — run apptainer/pull.sh if missing) --
/tmp/ctl-capture-873331/images/qdrant.sif
/tmp/ctl-capture-873331/images/elasticsearch.sif

-- file: /tmp/ctl-capture-873331/tenants/ctltest/bin/up.sh --
#!/usr/bin/env bash
# Start the dedicated stores for tenant 'ctltest'.
# Generated by apptainer/new-tenant.sh — do not edit; re-run it to regenerate.
#
# Instances: qdrant-ctltest (http :41001, grpc :41002)
#            elasticsearch-ctltest (http :41003, transport :41004)
set -euo pipefail
IMG="/tmp/ctl-capture-873331/images"
TDIR="/tmp/ctl-capture-873331/tenants/ctltest"

require_sif() {
    [[ -f "$1" ]] || { echo "ERROR: missing $1 — run apptainer/pull.sh"; exit 1; }
}

already_running() {
    apptainer instance list 2>/dev/null | awk 'NR>1 {print $1}' | grep -qx "$1"
}

# Seed an empty host bind dir from the image's copy of the same path.
seed_if_empty() {
    local sif="$1" container_path="$2" host_path="$3"
    if [[ -n "$(ls -A "$host_path" 2>/dev/null)" ]]; then return; fi
    echo "  seeding $host_path from $sif:$container_path"
    apptainer exec --bind "$host_path:/__seed" "$sif" \
        sh -c "cp -R $container_path/. /__seed/"
}

start() {
    local name="$1" sif="$2"; shift 2
    require_sif "$sif"
    if already_running "$name"; then
        echo "[$name] already running — skipping"
        return
    fi
    # Split remaining args at `--`: before are apptainer options,
    # after are runscript args (passed to the container CMD).
    local opts=() args=() mode=opts
    for a in "$@"; do
        if [[ "$a" == "--" ]]; then mode=args; continue; fi
        if [[ "$mode" == opts ]]; then opts+=("$a"); else args+=("$a"); fi
    done
    echo "[$name] starting"
    apptainer instance run "${opts[@]}" "$sif" "$name" "${args[@]}"
}

mmc=$(sysctl -n vm.max_map_count 2>/dev/null || echo 0)
if (( mmc < 262144 )); then
    echo "WARN: vm.max_map_count=$mmc (<262144) — Elasticsearch will fail to start."
    echo "      Fix once with: sudo sysctl -w vm.max_map_count=262144"
fi

# Apptainer has no --cwd flag and qdrant's CMD is CWD-relative — cd first.
# Ports via double-underscore envs (safe: no dots for the shell to mangle).
start qdrant-ctltest "$IMG/qdrant.sif" \
    --bind "$TDIR/qdrant/storage:/qdrant/storage" \
    --bind "$TDIR/qdrant/snapshots:/qdrant/snapshots" \
    --env QDRANT__SERVICE__HTTP_PORT=41001 \
    --env QDRANT__SERVICE__GRPC_PORT=41002 \
    -- /bin/sh -c 'cd /qdrant && exec ./entrypoint.sh'

# ES config must carry the image's files before first start (an empty bind
# breaks startup).
seed_if_empty "$IMG/elasticsearch.sif" /usr/share/elasticsearch/config \
    "$TDIR/elasticsearch/config"

# Dotted ES settings CANNOT go through --env (apptainer shell-sources it) —
# use native -E args, and call docker-entrypoint.sh directly: /bin/tini
# greedily parses -E as its own option.
start elasticsearch-ctltest "$IMG/elasticsearch.sif" \
    --bind "$TDIR/elasticsearch/data:/usr/share/elasticsearch/data" \
    --bind "$TDIR/elasticsearch/logs:/usr/share/elasticsearch/logs" \
    --bind "$TDIR/elasticsearch/config:/usr/share/elasticsearch/config" \
    --env ES_JAVA_OPTS="-Xms512m -Xmx512m" \
    -- /usr/local/bin/docker-entrypoint.sh eswrapper \
        -Ediscovery.type=single-node \
        -Expack.security.enabled=false \
        -Ehttp.port=41003 \
        -Etransport.port=41004

echo
apptainer instance list

-- file: /tmp/ctl-capture-873331/tenants/ctltest/bin/down.sh --
#!/usr/bin/env bash
# Stop the dedicated stores for tenant 'ctltest'. Idempotent.
# Generated by apptainer/new-tenant.sh — do not edit; re-run it to regenerate.
set -euo pipefail
for name in qdrant-ctltest elasticsearch-ctltest; do
    if apptainer instance stop "$name" 2>/dev/null; then
        echo "[$name] stopped"
    else
        echo "[$name] not running"
    fi
done

-- file: /tmp/ctl-capture-873331/tenants/ctltest/config/tenant.env --
# ==========================================================================
# tenant.env — tenant 'ctltest' (generated by apptainer/new-tenant.sh)
#
# Load with:   set -a; . /tmp/ctl-capture-873331/tenants/ctltest/config/tenant.env; set +a
# Never pipe through xargs — it strips the quotes around JSON values.
# JSON values MUST stay single-quoted (this file is shell-sourced).
# Re-running new-tenant.sh KEEPS an edited copy of this file (use --force
# to overwrite); it never re-generates the API keys or the DB password.
# ==========================================================================

# --- identity / authz -----------------------------------------------------
API_KEYS='["<GENERATED:API_KEY_USER>","<GENERATED:API_KEY_ADMIN>"]'
API_KEY_TENANTS='{"<GENERATED:API_KEY_USER>":"ctltest","<GENERATED:API_KEY_ADMIN>":"ctltest"}'
API_KEY_ROLES='{"<GENERATED:API_KEY_USER>":"user","<GENERATED:API_KEY_ADMIN>":"admin"}'
DEFAULT_ROLE=user
IDENTITY_PROVIDER=none
# To accept bearer identities instead, pick one and fill in the pins:
# IDENTITY_PROVIDER=oidc
# IDENTITY_OIDC_ISSUER=https://accounts.example.com
# IDENTITY_OIDC_CLIENT_IDS='["<client-id>"]'
# IDENTITY_PROVIDER=bvbrc    # issuer allowlist is pinned in config.py
MAX_COLLECTIONS=100
USER_STORE_BACKEND=sqlite
USER_STORE_PATH=/tmp/ctl-capture-873331/tenants/ctltest/state/ragstack_users.db
ACL_BACKFILL_OWNER=legacy:admin

# --- dedicated stores (ADR-0005: data at rest defines the tenant) ---------
VECTOR_BACKEND=qdrant
QDRANT_URL=http://localhost:41001
TEXT_BACKEND=elasticsearch
ELASTICSEARCH_URL=http://localhost:41003
GRAPH_BACKEND=disabled
JOB_STORE_BACKEND=sqlite
JOB_STORE_PATH=/tmp/ctl-capture-873331/tenants/ctltest/state/ragstack_jobs.db
COLLECTION_STORE_BACKEND=sqlite
COLLECTION_STORE_PATH=/tmp/ctl-capture-873331/tenants/ctltest/state/ragstack_collections.db
COLLECTION_MANIFEST_DIR=/tmp/ctl-capture-873331/tenants/ctltest/manifests
# COLLECTIONS_FILE=/tmp/ctl-capture-873331/tenants/ctltest/config/collections.json   # optional one-time registry seed

# --- embedding / rerank (shared stateless compute, per ADR-0005) ----------
EMBEDDING_API=openai
EMBEDDING_ENDPOINTS='["http://localhost:9001","http://localhost:9002"]'
EMBEDDING_MODEL=Salesforce/SFR-Embedding-Mistral
EMBEDDING_MODEL_DIM=4096
EMBEDDING_SIDECAR_URL=http://localhost:50053
CROSSENCODER_SIDECAR_URL=http://localhost:50052

# --- safety ----------------------------------------------------------------
REQUIRE_DURABLE_BACKENDS=true
INGEST_ROOT=/tmp/ctl-capture-873331/tenants/ctltest/ingest
MAX_DOCUMENT_BYTES=50000000

# --- API -------------------------------------------------------------------
# PORT is informational only — the API's Settings does not read it; uvicorn
# needs --port 41000 explicitly (see the start command below/in the plan).
PORT=41000
LOG_LEVEL=INFO

-- start/stop --
start stores:  /tmp/ctl-capture-873331/tenants/ctltest/bin/up.sh
stop stores:   /tmp/ctl-capture-873331/tenants/ctltest/bin/down.sh
start API:     set -a; . /tmp/ctl-capture-873331/tenants/ctltest/config/tenant.env; set +a
               uvicorn ragstack.api.main:app --host 0.0.0.0 --port 41000
