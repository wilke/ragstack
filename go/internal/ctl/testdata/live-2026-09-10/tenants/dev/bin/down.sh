#!/usr/bin/env bash
# Stop the dedicated stores for tenant 'dev'. Idempotent.
# Generated by apptainer/new-tenant.sh — do not edit; re-run it to regenerate.
set -euo pipefail
for name in qdrant-dev elasticsearch-dev; do
    if apptainer instance stop "$name" 2>/dev/null; then
        echo "[$name] stopped"
    else
        echo "[$name] not running"
    fi
done
