# provision.env — tenant 'dev' (written by apptainer/new-tenant.sh)
# Persists provisioning choices across re-runs; flags on a later run update it.
TENANT_ES_HEAP=512m
TENANT_STORE_KIND=sqlite
TENANT_PG_HOST=
TENANT_PG_PORT=
