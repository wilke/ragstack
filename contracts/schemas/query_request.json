{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "query_request.json",
  "title": "QueryRequest",
  "description": "Full RAG pipeline query request with rewrite, retrieve, rerank, and generate.",
  "type": "object",
  "required": ["query"],
  "properties": {
    "query": {
      "type": "string"
    },
    "top_k": {
      "type": "integer",
      "default": 5
    },
    "rewrite_strategies": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": ["passthrough"]
    },
    "filters": {
      "type": "object",
      "default": {}
    },
    "use_graph": {
      "type": "boolean",
      "default": true
    },
    "stream": {
      "type": "boolean",
      "default": false
    },
    "rerank": {
      "description": "Per-request rerank control. null (default) preserves the server-wide default; false forces a rerank skip; true is a no-op when no reranker is wired.",
      "type": ["boolean", "null"],
      "default": null
    },
    "rerank_candidates": {
      "description": "Override for the candidate-pool depth fed to the reranker. null (default) uses the server default (max(top_k, server's rerank_candidates)).",
      "type": ["integer", "null"],
      "minimum": 1,
      "default": null
    },
    "collection": {
      "description": "Registry collection id to query. null (default) uses the default collection; an unknown id returns 404. See GET /v1/collections.",
      "type": ["string", "null"],
      "default": null
    },
    "retrieval_mode": {
      "description": "Retrieval legs: hybrid (dense + BM25, RRF-fused), vector (dense only), or bm25 (sparse only). The graph leg is orthogonal (use_graph).",
      "type": "string",
      "enum": ["hybrid", "vector", "bm25"],
      "default": "hybrid"
    },
    "llm": {
      "description": "Registered model id to generate the answer with for THIS request only (global assignment untouched; retrieval unchanged). Unknown id 404; non-llm model 400. See GET /v1/models/available.",
      "type": ["string", "null"],
      "default": null
    },
    "reranker": {
      "description": "Registered model id to rerank with for THIS request only. Unknown id 404; non-reranker model 400. See GET /v1/models/available.",
      "type": ["string", "null"],
      "default": null
    }
  },
  "additionalProperties": false
}
