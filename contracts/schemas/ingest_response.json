{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "ingest_response.json",
  "title": "IngestResponse",
  "description": "Response for a document ingestion job.",
  "type": "object",
  "required": ["job_id", "status"],
  "properties": {
    "job_id": {
      "type": "string"
    },
    "status": {
      "type": "string"
    },
    "chunk_ids": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "default": []
    },
    "items": {
      "type": ["object", "null"],
      "description": "Per-document progress for batch/directory ingests.",
      "properties": {
        "total": { "type": "integer" },
        "completed": { "type": "integer" },
        "failed": { "type": "integer" },
        "pending": { "type": "integer" }
      },
      "additionalProperties": false
    },
    "collection": {
      "type": ["string", "null"],
      "description": "The collection this job actually targets. Only meaningful when the request omitted `collection` \u2014 that is the case where the SERVER chose. The id is always one from the caller's own GET /v1/collections listing, so echoing it discloses nothing."
    }
  },
  "additionalProperties": false
}
