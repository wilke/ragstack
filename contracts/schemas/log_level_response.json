{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "log_level_response.json",
  "title": "LogLevelResponse",
  "description": "The log level actually in effect in THIS process, and how it got there. Returned by GET, PUT and DELETE /v1/admin/log-level.\n\nThe configured/effective split is the point of this shape. GET /v1/config echoes the RAW `LOG_LEVEL` string, so a value the server rejected at startup (`LOG_LEVEL=verbose`, say) is reported there while INFO is what is actually in force. Here `configured_level` is that same raw string, `configured_level_resolved` is what it resolves to, and `effective_level` is what the root logger is set to right now — which differs from both whenever a runtime override is in place.\n\nRuntime state is PROCESS-LOCAL and resets on restart. That is deliberate: a debugging session cannot silently become the permanent configuration, and the way to make a level stick is to change `LOG_LEVEL` and restart. `pid` is reported so an operator can tell which process answered. Every production launch today is a single uvicorn process with no `--workers`, so one call reaches the one process that serves every request; if `--workers` were ever added, each worker would keep its own runtime level and a single call would change exactly one of them at random. Nothing here is shared between processes.\n\nA `ttl_seconds` expiry is reported by `auto_revert_pending`, `expires_at` and `expires_in_seconds`. It is process-local too, and it is dropped at shutdown without firing — a restart reverts the level anyway, so an expiry that outlived its process would have nothing left to revert.",
  "type": "object",
  "required": [
    "pid",
    "configured_level",
    "configured_level_resolved",
    "effective_level",
    "runtime_override",
    "changed_at",
    "changed_by",
    "dampening_active",
    "dampen_loggers",
    "loggers",
    "logger_override_count",
    "max_logger_overrides",
    "auto_revert_pending",
    "ttl_seconds",
    "expires_at",
    "expires_in_seconds",
    "max_ttl_seconds"
  ],
  "properties": {
    "pid": {
      "description": "OS process id that answered. Runtime log state lives in this process only.",
      "type": "integer"
    },
    "configured_level": {
      "description": "The RAW `LOG_LEVEL` setting, exactly as GET /v1/config echoes it — including a value the server rejected. Compare with `configured_level_resolved` to see whether it was understood.",
      "type": "string"
    },
    "configured_level_resolved": {
      "description": "What `configured_level` resolves to after upper-casing and validation; INFO when it was unrecognised. This is what a restart would return the process to.",
      "type": "string",
      "enum": [
        "CRITICAL",
        "FATAL",
        "ERROR",
        "WARNING",
        "WARN",
        "INFO",
        "DEBUG",
        "NOTSET"
      ]
    },
    "effective_level": {
      "description": "The root logger's level RIGHT NOW. Equals `configured_level_resolved` unless `runtime_override` is true. It governs every logger that inherits from root — which is not all of them: `uvicorn.*` sets `propagate=False` and carries its own handlers, so it consults its own level and is unaffected by this field. Name a uvicorn logger in `loggers` to reach it.",
      "type": "string",
      "enum": [
        "CRITICAL",
        "FATAL",
        "ERROR",
        "WARNING",
        "WARN",
        "INFO",
        "DEBUG",
        "NOTSET"
      ]
    },
    "runtime_override": {
      "description": "True when a PUT has changed the root level in this process since it started (or since the last DELETE). False means `effective_level` came from configuration.",
      "type": "boolean"
    },
    "changed_at": {
      "description": "ISO-8601 UTC timestamp of the last runtime change (PUT, DELETE, or a `ttl_seconds` auto-revert); empty string if this process has never had one. Empty rather than null, matching `UserRoleRecord.role_set_at`.",
      "type": "string"
    },
    "changed_by": {
      "description": "Tenant of the admin principal who made that last change; empty string if there has never been one. A NON-empty `changed_at` with an EMPTY `changed_by` is the signature of an auto-revert: the process changed its own level and no principal typed it. The `audit=expired` WARNING line names the principal that armed the TTL, and is the durable record — this field is a convenience and dies with the process.",
      "type": "string"
    },
    "dampening_active": {
      "description": "True when the loggers in `dampen_loggers` are pinned to WARNING. They are pinned whenever the effective level is above DEBUG and released at DEBUG, so that turning the root level down actually turns the HTTP transports on — which is the whole reason to turn it down. A single /v1/query makes 5 outbound HTTP calls minimum and up to ~14 on the multi-collection path, one httpx INFO line each, so leaving them un-damped at INFO would bury our own lines at 1:5.",
      "type": "boolean"
    },
    "dampen_loggers": {
      "description": "The configured dampen set (`LOG_DAMPEN_LOGGERS`). A name here can still be overridden explicitly; the override wins and the entry in `loggers` then reads `source: override`.",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "loggers": {
      "description": "Every logger this endpoint currently touches: the runtime overrides and the dampen set, unioned. `level` is that logger's OWN level (NOTSET = it inherits from root). The per-logger overrides are exactly the entries with `source: override`.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/LoggerLevel"
      }
    },
    "logger_override_count": {
      "description": "How many runtime per-logger overrides are in force.",
      "type": "integer"
    },
    "max_logger_overrides": {
      "description": "The cap on that count. A PUT that would exceed it is a 422 and changes nothing.",
      "type": "integer"
    },
    "auto_revert_pending": {
      "description": "True when a `ttl_seconds` expiry is armed and this process will change its own log level when it fires. An operator MUST be able to see this: the level is going to move under them, and nothing else in this response would say so. False whenever `expires_in_seconds` is null, and vice versa.",
      "type": "boolean"
    },
    "ttl_seconds": {
      "description": "The TTL that was requested on the change now in force, or null when none is armed. This is the value as sent, not a countdown — `expires_in_seconds` is the countdown.",
      "type": ["integer", "null"]
    },
    "expires_at": {
      "description": "ISO-8601 UTC wall-clock deadline of the pending auto-revert; empty string when none is armed. Empty rather than null, matching `changed_at`. The countdown in `expires_in_seconds` is measured on a MONOTONIC clock and is the authority — this field is for a human reading a log or a dashboard, and a wall-clock step (NTP, a suspended VM) moves it without moving the deadline.",
      "type": "string"
    },
    "expires_in_seconds": {
      "description": "Seconds remaining until the auto-revert fires, rounded UP so a freshly armed 600s TTL reads 600; null when none is armed. Clamped at 0 rather than going negative: 0 with `auto_revert_pending: true` means the deadline has passed and the callback has not run yet, which on a busy loop is a moment, not a state.",
      "type": ["integer", "null"]
    },
    "max_ttl_seconds": {
      "description": "The upper bound on `ttl_seconds` (the lower bound is 1). A PUT outside the range is a 422 and changes nothing. Reported the way `max_logger_overrides` is, so the bound is discoverable at runtime rather than only in this document.",
      "type": "integer"
    }
  },
  "additionalProperties": false,
  "$defs": {
    "LoggerLevel": {
      "type": "object",
      "required": [
        "name",
        "level",
        "source"
      ],
      "properties": {
        "name": {
          "description": "The logger's dotted name.",
          "type": "string"
        },
        "level": {
          "description": "The level set on this logger itself. NOTSET means it inherits from root rather than meaning 'silent'.",
          "type": "string",
          "enum": [
            "CRITICAL",
            "FATAL",
            "ERROR",
            "WARNING",
            "WARN",
            "INFO",
            "DEBUG",
            "NOTSET"
          ]
        },
        "source": {
          "description": "`override` — a runtime per-logger override set through this endpoint. `dampen` — a member of the configured dampen set, at whatever the current root level implies.",
          "type": "string",
          "enum": [
            "override",
            "dampen"
          ]
        }
      },
      "additionalProperties": false
    }
  }
}
