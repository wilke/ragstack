{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "service_accounts_response.json",
  "title": "ServiceAccountsResponse",
  "description": "The registered service accounts (GET /v1/admin/service-accounts), oldest first. Disabled accounts are included — disabling is soft state, never a row deletion, so the audit trail survives.",
  "type": "object",
  "required": ["service_accounts"],
  "properties": {
    "service_accounts": {
      "type": "array",
      "items": { "$ref": "service_account_record.json" }
    }
  },
  "additionalProperties": false
}
