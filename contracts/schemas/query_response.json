{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "query_response.json",
  "title": "QueryResponse",
  "description": "Query response containing the generated answer, source chunks, and rewritten queries.",
  "type": "object",
  "required": [
    "answer",
    "sources",
    "rewritten_queries"
  ],
  "properties": {
    "answer": {
      "type": "string"
    },
    "sources": {
      "type": "array",
      "items": {
        "$ref": "source.json"
      }
    },
    "rewritten_queries": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "template": {
      "description": "The template id the answer was generated with; absent when none was used.",
      "type": "string"
    },
    "template_version": {
      "description": "That template's declared version. Bumped on any content change.",
      "type": "integer"
    },
    "template_hash": {
      "description": "Content hash of the template SOURCE, computed at load \u2014 not of a rendered body, which would depend on this request's template_vars and could not be computed at load at all. Present only when a templated generation actually produced the answer. See prompt_templates_response.json.",
      "type": "string"
    },
    "model": {
      "description": "The registered model id the answer was actually generated with, after the per-request `llm` override and the server default are resolved. Echoed rather than assumed: a template does NOT pin a model (ADR-0008 decision 5), so this is how a degraded result is attributed to a swapped default instead of being mistaken for a prompt regression.",
      "type": "string"
    },
    "truncated": {
      "description": "True when the model hit its token ceiling mid-answer; omitted otherwise. Present only on the templated path, where it matters most: a truncated TSV table has lost ROWS, and nothing in the answer text says so \u2014 the row count silently tracks how verbose the model happened to be per row. Raise the template's max_output_tokens (or the server's llm_max_output_tokens) if this appears routinely.",
      "type": "boolean"
    }
  },
  "additionalProperties": false
}
